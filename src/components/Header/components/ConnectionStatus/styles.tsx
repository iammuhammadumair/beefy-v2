import { theme } from '../../../../theme';

export const styles = () => ({
  container: {
    display: 'flex',
    backgroundColor: theme.palette.background.cta,
    alignItems: 'center',
    borderRadius: '50px',
  },
  open: {
    backgroundColor: theme.palette.background.cta,
  },
});
